#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * @n: number to test
 *
 * Return: n if n is greater than 0, -n otherwise
 */
int _abs(int)
{
	int n;

	if (n > 0)
		return (n);
	else
		return(-n);
}
