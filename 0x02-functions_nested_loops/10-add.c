#include "main.h"
/**
 * add - adds two integers and returns the result
 * @x: the first entered integer
 * @y: the second entered integer
 * Return: The result of the addition of 'x' and 'y'
 */
int add(int x, int y)
{
	return (x + y);
}
